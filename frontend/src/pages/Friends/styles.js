import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  root: {
    width: "100%",
  },
  searchContainer: {
    display: "flex",
    margin: 20,
  },
  friendsContainer: {
    display: "flex",
    flexGrow: 1,
    width: "100%",
  },
}));
